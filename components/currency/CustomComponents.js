import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const MyCurContainer = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
  width: "100vw",
  minHeight: "100vh",
  p: 4,
});
